using unilang.common;
using static Cities.city;

namespace Cities.editors {
    public static class ArchetypeConstructor {
        public static readonly vec<archetype> archetypes = new vec<archetype>(16, 16);
    }
}